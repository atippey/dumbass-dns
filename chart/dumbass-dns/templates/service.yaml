apiVersion: v1
kind: Service
metadata:
  name: {{ include "dumbass-dns.fullname" . }}
  labels:
    {{- include "dumbass-dns.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dumbass-dns.selectorLabels" . | nindent 4 }}
